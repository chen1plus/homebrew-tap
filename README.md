# Homebrew Tap

This is my personal Homebrew tap. To add the tap, run:

```sh
brew tap chen1plus/tap
```

The tap includes:

- `tongwen` — an OpenAI-compatible local Chinese Converter endpoint.
- `tytm` — Typora theme manager.
